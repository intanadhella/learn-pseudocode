START
OUTPUT 'Masukkan nilai x'
INPUT user memasukkan nilai x
IF x < 60
    OUTPUT 'kurang'
ELSE IF x < 75
    OUTPUT 'cukup'
ELSE IF x < 89
    OUTPUT 'baik'
ELSE IF x <= 100
    OUTPUT 'sangat baik'
STOP